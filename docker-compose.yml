services:
  proddyt-tools:
    build: .
    container_name: proddyt-tools
    restart: unless-stopped
    ports:
      - "3001:3001"
    networks:
      - proxy

networks:
  proxy:
    external: true
